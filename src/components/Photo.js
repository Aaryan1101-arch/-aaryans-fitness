// Removed: gallery photos are now managed via Sanity CMS.
// See src/sanity/fallbackContent.js for the bundled defaults
// and studio/schemas/galleryItem.js for the editable schema.
module.exports = {};
